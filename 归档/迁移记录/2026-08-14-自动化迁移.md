# 2026-08-14 自动化迁移

## 当前任务

| 任务 | 时间 | 状态 | 用户确认边界 |
| --- | --- | --- | --- |
| 周回顾整理 | 每周一 02:00 | 已启用 | 只生成草案和机械检查 |
| 周回顾确认 | 每周一 20:00 | 已启用 | 一次只讨论一个关键决定 |
| 月度回顾整理 | 每月 1 日 02:30 | 已启用 | 不决定本月方向和里程碑 |
| 月度方向确认 | 每月 1 日 20:00 | 已启用 | 用户明确确认后才能更新正式规划 |
| Agent 招聘周报 | 每周二、周五 02:00 | 已启用 | 只维护岗位事实；不生成个人求职计划 |

## 已移除

- 删除旧的每日同步与反思任务。该任务依赖旧目录、旧日卡和 Hermes QQBot，不进入新系统。

## Git 安全

- 凌晨任务提交前必须运行 `.agents/skills/review-maintainer/scripts/stage_safe.py --root .`。
- 不允许 `git add -A`、强推或自动解决冲突。
- 远端分叉、验证失败、异常大文件或冲突时停止。

## 通信边界

- 所有结果留在 Codex 任务中。
- 没有任务调用 Hermes 或发送 QQ。
- 更直接的 QQ 方式和消息格式已经作为低优先级已确认事项进入总清单，但本轮不实施。

## 试跑记录

| 任务 | 试跑结果 | 证据与剩余边界 |
| --- | --- | --- |
| 周回顾整理 | 通过 | 生成 2026-W32 草案并提交推送，提交 `bcff9f6` |
| 月度回顾整理 | 通过 | 生成 2026-07 草案并提交推送，提交 `7000925` |
| 周回顾确认 | 通过 | 只提出一个关键问题和 3 个方案，没有写文件或自动确认 |
| 月度方向确认 | 通过 | 只提出一个关键问题和 3 个方案，没有继承旧计划 |
| Agent 招聘周报新路径 | 通过 | 全新只读 Codex 会话读取新路径；127 条记录、126 条 active、53 个相对链接均正常 |

周/月整理试跑期间发现工作区写入型 `codex exec` 可能在外层返回后继续运行，已中断残留进程并确认没有并发提交。当前自动任务直接由 Codex 调度，不依赖该外层试跑方式。

真实调度触发仍需等各任务首次按计划运行后确认；上述通过不等于真实调度、官网扫描或自动提交推送已经发生。
